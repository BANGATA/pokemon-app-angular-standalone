import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'pokemon-app-angular-standalone',
  webDir: 'www'
};

export default config;
